# Worker process - polls /api/copy-trade on the web app
# Polymarket blocks US/EU – use Mumbai (bom); India is permitted
app = 'polymarket-trader-worker'
primary_region = 'bom'

[build]
  dockerfile = "Dockerfile.worker"

[env]
  APP_BASE_URL = "https://polymarket-trader.fly.dev"

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
